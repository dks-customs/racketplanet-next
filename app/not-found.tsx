import notFoundMetadata from "../util/notFoundMetadata";

export default function NotFound() {
  return <main className="not-found layout-container">404</main>;
}

export const metadata = notFoundMetadata("");
